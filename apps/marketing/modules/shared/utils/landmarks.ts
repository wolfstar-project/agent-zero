/**
 * Id of the `<main>` landmark. Shared between the layout that renders it and the skip link that
 * targets it, so the two can never drift apart into a link that jumps nowhere.
 */
export const mainContentId = 'main-content';
