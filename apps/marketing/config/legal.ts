/**
 * Date the shipped legal documents were last revised, as an ISO 8601 calendar date.
 *
 * Hardcoded rather than derived from the build: "last updated" has to mean the last time a human
 * changed the text, and a build timestamp would silently refresh it on every deploy.
 */
export const legalLastUpdated = '2026-08-15';
